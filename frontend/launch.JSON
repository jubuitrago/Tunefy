{
    "version": "0.2.0",
    "configurations": [ 
        {
            "name": "Chrome",
            "type": "chrome",
            "request": "launch",
            "url": "${process.env.REACT_APP_FRONTEND_URL}",     // create-react-app's default port 3000
            "webRoot": "${workspaceRoot}/src"
        }
    ]
}